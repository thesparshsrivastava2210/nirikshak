[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nirikshak"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic"
]

[tool.vercel]
entrypoint = "frontend/api/[...catchall].py"
