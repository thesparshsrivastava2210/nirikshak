[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nirikshak-frontend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic"
]

[tool.vercel]
entrypoint = "api.[...catchall]:app"
